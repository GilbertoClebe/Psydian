{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "psydian": "concurrently \"uvicorn backend.main:app --reload --port 8000\" \"npm run tauri dev\""
  }
}
